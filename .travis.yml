# See also https://github.com/hvr/multi-ghc-travis for more information
language: c
env:
 - CABALVER=1.18 GHCVER=7.8.4
 - CABALVER=1.22 GHCVER=7.10.1
 - CABALVER=1.22 GHCVER=7.10.2
 - CABALVER=1.22 GHCVER=8.0.1

before_install:
 - travis_retry sudo add-apt-repository -y ppa:hvr/ghc
 - travis_retry sudo apt-get update
 - travis_retry sudo apt-get install cabal-install-$CABALVER ghc-$GHCVER
 - export PATH=/opt/ghc/$GHCVER/bin:/opt/cabal/$CABALVER/bin:$PATH

install:
 - cabal --version
 - echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo '?')]"
 - travis_retry cabal update
 - cabal install --only-dependencies --enable-tests --enable-benchmarks

script:
 - if [ -f configure.ac ]; then autoreconf -i; fi
 - cabal configure --enable-tests --enable-benchmarks -v2
 - cabal build
 - cabal test
 - cabal check
 - cabal sdist
 - SRC_TGZ=$(cabal info . | awk '{print $2;exit}').tar.gz &&
   (cd dist && cabal install --force-reinstalls "$SRC_TGZ")
deploy:
  provider: hackage
  username: AlexanderThiemann
  skip_cleanup: true
  on:
    condition: "$GHCVER = 8.0.1"
    tags: true
  password:
    secure: M1Id7L3u6ElHJ2HStUYaEpUI/u0Z69LD5SaspnjXFcvMlLVhs7ZcMshslIGTJ/43kikcPYUorZmSOXS4rJR17nfZSRiZgpMjC1+zqeCzkXe/cPgM1GguWaMeDNVtWtKVvk6ph07rUmmmx1wR1DpHwM1E/c9AUfnDc2+VXB1nf64=
